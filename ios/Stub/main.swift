// Placeholder entry point. Xcode needs at least one source file to produce a
// linkable, signable app bundle; the real executable (built by cargo for
// aarch64-apple-ios) is swapped in by the "Build Rust binary" script phase
// below, before Xcode code-signs the product.
